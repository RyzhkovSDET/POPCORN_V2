"""
Все статические константы и настройки таблицы watchlist в одном месте.
main.py и другие модули ui/ импортируют отсюда, а не хранят копии.
"""

VALID_QUOTES = ("USDT", "BUSD", "BTC", "ETH")

# ---------------------------------------------------------------------------
# Скорость обновления
# ---------------------------------------------------------------------------
REFRESH_SEC = 10           # цена / Изм / 1-24ч / Funding / OI -- быстрый блок
SLOW_METRICS_TTL_SEC = 60  # RSI / MACD / ATR / Trend / Сигнал / Pattern / Мин / Макс -- медленный блок

# ---------------------------------------------------------------------------
# Прочие константы расчётов
# ---------------------------------------------------------------------------
MAX_ROWS = 12
PATTERN_LOOKBACK = 30

# "Медленные" индикаторы (RSI/EMA/MACD/ATR/Pattern) считаются на выбранном
# пользователем таймфрейме -- дефолт 30m. Раньше был жёстко зашит 1m/100
# баров (~1.5ч истории), что было слишком шумно.
SLOW_METRICS_INTERVAL = "30m"          # дефолт при первом запуске
SLOW_METRICS_CANDLES_LIMIT = 100       # 100 баров хватает на стабильные EMA(50)/RSI(14)/MACD на любом из таймфреймов ниже

# Доступные таймфреймы для RSI/MACD/EMA/ATR (подпись, интервал). Ограничены
# теми интервалами, что есть у ВСЕХ бирж-фолбэков (Binance/Coinbase/Kraken) --
# иначе при переключении на биржу без такого интервала (например 8h есть
# только у Binance) весь блок индикаторов вернул бы "н/д".
SLOW_METRICS_TIMEFRAME_OPTIONS = [
    ("30м", "30m"),
    ("1ч", "1h"),
    ("4ч", "4h"),
    ("1д", "1d"),
]

HTF_INTERVAL = "15m"
HTF_LIMIT = 100

# Горизонт "4ч Прогноза" теперь настраиваемый. Короткие горизонты (2ч/4ч)
# считаются на 15-минутках -- это ок. Длинные (6ч/12ч/1д) на 15м были бы
# слишком шумными для такого дальнего прогноза, поэтому считаются на
# часовых свечах -- более сглаженно, надёжнее для длинного горизонта.
# Формат кортежа: (подпись, часы горизонта, интервал данных, лимит свечей).
FORECAST_HORIZON_OPTIONS = [
    ("2ч", 2, "15m", 100),
    ("4ч", 4, "15m", 100),
    ("6ч", 6, "1h", 150),
    ("12ч", 12, "1h", 150),
    ("1д", 24, "1h", 150),
]
FORECAST_HORIZON_DEFAULT_LABEL = "4ч"

# Окно для столбца Trend -- на сколько свечей назад сравнивать цену.
# 1 = максимально чувствительно (шумно), 10 = максимально сглажено.
TREND_WINDOW_OPTIONS = [
    ("1 свеча", 1),
    ("3 свечи", 3),
    ("5 свечей", 5),
    ("7 свечей", 7),
    ("10 свечей", 10),
]
TREND_WINDOW_DEFAULT = 5

BREAKOUT_LOOKBACK_DAYS = 3
DAILY_CANDLES_LIMIT = 10
HOURLY_CANDLES_LIMIT = 52          # хватает на окно 24ч х 2 + запас
VOLUME_WINDOWS_HOURS = (1, 3, 6, 24)
ANALYSIS_LOOKBACK_DAYS = 90
FIBONACCI_SWING_DAYS = 30
CHART_INTERVAL = "30m"     # таймфрейм свечей на графике по умолчанию (можно менять в UI)
CHART_CANDLES_LIMIT = 150
# Доступные таймфреймы графика в UI: (подпись, интервал, лимит свечей)
CHART_TIMEFRAME_OPTIONS = [
    ("1м", "1m", 180),
    ("5м", "5m", 180),
    ("15м", "15m", 150),
    ("30м", "30m", 150),
    ("1ч", "1h", 150),
    ("4ч", "4h", 150),
    ("1д", "1d", 120),
]
SCREENER_TTL_SEC = 600     # 10 минут -- как часто обновляются блоки скринера
SCREENER_TOP_N = 6
SCREENER_RISE_THRESHOLD_PCT = 3.0    # "сильно выросшие" -- порог в % за 24ч
SCREENER_DROP_THRESHOLD_PCT = -3.0   # "сильно упавшие" -- порог в % за 24ч

# ---------------------------------------------------------------------------
# Столбцы таблицы watchlist
# ---------------------------------------------------------------------------
# Единый список (ключ, ширина) -- порядок здесь = порядок в таблице.
# COL_KEYS/COL_WIDTHS собираются из него автоматически, чтобы порядок и
# ширины никогда не могли разъехаться друг с другом.
_COLUMNS = [
    ("Coin", 1.5),
    ("Price", 0.8),
    ("Изм", 0.85),
    ("RSI", 1.0),
    ("Trend", 0.5),
    ("EMA", 0.9),
    ("MACD", 0.85),
    ("Pattern", 0.75),
    ("Сигнал", 1.1),
    ("OI", 1.1),
    ("Funding", 0.95),
    ("ATR", 1.0),
    ("Volume", 0.65),
    ("Общий объём", 0.95),
    ("1ч", 0.6),
    ("3ч", 0.6),
    ("6ч", 0.6),
    ("24ч", 0.65),
    ("Прогноз", 1.0),
    ("Пробой", 1.0),
    ("", 0.4),
]
COL_KEYS = [key for key, _ in _COLUMNS]
COL_WIDTHS = [width for _, width in _COLUMNS]

# Короткая справка на русском для подсказки при наведении на заголовок столбца
COLUMN_TOOLTIPS = {
    "Coin": "Кружочек -- выбрать для анализа/графика. Таблетка с именем -- цикл цветовой метки.",
    "Price": "Текущая цена.",
    "Изм": "Изменение цены за 24ч.",
    "RSI": "Сила движения: <30 перепроданность (ПОК), >70 перекупленность (ПРД).",
    "Trend": "Направление цены за окно (выбирается над таблицей): ▲ рост, ▼ падение.",
    "EMA": "Быстрая EMA(20) относительно медленной EMA(50): выше = бычий тренд.",
    "MACD": "MACD против сигнальной линии: выше = бычий импульс.",
    "ATR": "Волатильность в % от цены. Выше = сильнее движения, риск шире.",
    "Volume": "Объём последней свечи. Цвет -- выше или ниже предыдущей.",
    "Общий объём": "24ч объём со всех бирж (CoinGecko). Цвет -- растёт или падает.",
    "1ч": "Изменение объёма за последний час к предыдущему часу.",
    "3ч": "Изменение объёма за последние 3ч к предыдущим 3ч.",
    "6ч": "Изменение объёма за последние 6ч к предыдущим 6ч.",
    "24ч": "Изменение объёма за последние 24ч к предыдущим 24ч.",
    "Funding": "Ставка финансирования фьючерса. Контрарный сигнал перегрева.",
    "OI": "Открытый интерес по фьючерсу + тренд за час.",
    "Сигнал": "Скоринг 0-100: RSI + EMA + MACD. Ориентир, не рекомендация.",
    "Pattern": "Сверху сигнал, снизу краткое имя паттерна. Наведи на саму ячейку -- полное название.",
    "Пробой": "Пробой мин/макс за 3 дня: направление и сколько раз подряд.",
    "Прогноз": "Прогноз на выбранный горизонт (селектор над таблицей): EMA/RSI/ADX + паттерн.",
    "": "",
}